<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.tabwu.health.mapper.OrderMapper">

    <insert id="addOrderOnline" parameterType="com.tabwu.health.entity.Order" useGeneratedKeys="true" keyProperty="id">
        insert into t_order (memberId,orderDate,orderType,orderStatus,setmealId)
        values (memberId,orderDate,orderType,orderStatus,setmealId)
    </insert>

    <select id="findOrderByCondition" parameterType="com.tabwu.health.entity.Order" resultType="com.tabwu.health.entity.Order">
        select * from t_order where
        <if test="memberId != 0">
            and memberId = #{memberId}
        </if>
        <if test="orderDate != null">
            and orderDate = #{orderDate}
        </if>
        <if test="setmealId != null">
            and setmealId = #{setmealId}
        </if>
    </select>

    <select id="findById" parameterType="int" resultType="com.tabwu.health.entity.Order">
        select * from t_order where id = #{orderId}
    </select>

    <select id="getTodayOrder" parameterType="string" resultType="int">
        select count(*) from t_order where orderDate = #{today}
    </select>

    <select id="getAllOrder"  resultType="int">
        select count(*) from t_order
    </select>

    <select id="getBeforeThisWeekAllOrder" parameterType="string" resultType="int">
        select count(*) from t_order where orderDate &gt;= #{thisWeekMonday}
    </select>

    <select id="getBeforeThisMonthAllOrder" parameterType="string" resultType="int">
        select count(*) from t_order where orderDate &gt;= #{firstDay4ThisMonth}
    </select>

    <select id="getTodayNumberTrue" parameterType="string" resultType="int">
        select count(*) from t_order where orderDate = #{today} and orderStatus = '已到诊'
    </select>

    <select id="getWeekNumberTrue" parameterType="string" resultType="int">
        select count(*) from t_order where orderDate &gt;= #{thisWeekMonday} and orderStatus = '已到诊'
    </select>

    <select id="getMonthNumberTrue" parameterType="string" resultType="int">
        select count(*) from t_order where orderDate &gt;= #{firstDay4ThisMonth} and orderStatus = '已到诊'
    </select>

    <!--热门套餐，查询前4条-->
    <select id="getHotSetmeal" resultType="map">
        select
            s.name,
            count(o.id) setmeal_count,
            count(o.id)/(select count(id) from t_order) proportion
        from t_order o inner join t_setmeal s on s.id = o.setmeal_id
        group by o.setmeal_id
        order by setmeal_count desc
        limit 0,4
    </select>
</mapper>