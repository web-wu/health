<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.tabwu.health.mapper.OrderSettingMapper">
    <select id="checkOrderSettingByOrderDate" resultType="long" parameterType="string">
        select count(*) from t_ordersetting where orderDate = #{orderDate};
    </select>

    <insert id="addOrderSetting" parameterType="com.tabwu.health.entity.OrderSetting">
        insert into t_ordersetting(orderDate, number, reservations) values (#{orderDate},#{number},#{reservations});
    </insert>

    <update id="updateOrderSetting" parameterType="com.tabwu.health.entity.OrderSetting">
        update t_ordersetting set number = #{number} where orderDate = #{orderDate};
    </update>
</mapper>